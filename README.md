# Portfolio CMS setup

Copy `.env.example` to `.env.local`, then configure MongoDB, Google OAuth, the admin email allow-list, `AUTH_SECRET`, and your ImgBB API key. In Google Cloud, add `/api/auth/callback` to the OAuth client's authorized redirect URIs. Visit `/admin/login` to sign in.
